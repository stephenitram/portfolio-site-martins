/*new code*/
Fixed header adjustment header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 1000;
}

/* Prevent content overlap below the fixed header */
.hero {
  padding-top: calc(6rem + 80px); /* 6rem existing + approx. header height */
}

/* Add to end of your styles.css */

/* Ensure header stays fixed and hamburger is visible */
header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 1000;
}

/* Adjust hero padding so it's not hidden under header */
.hero {
  padding-top: calc(6rem + 80px); /* adjust if needed */
}

/* Anchor offset fix for other sections */
section[id]:not(.hero) {
  scroll-margin-top: 100px;
}

/* Mobile-specific fixes */
@media (max-width: 768px) {
  header {
    padding: 1.5rem 2rem;
  }

  .main-nav {
    height: 60px;
    align-items: center;
  }

  .hamburger {
    z-index: 1002;
  }
}
